; Intentionally almost empty.
;
; All bindings live in Enhanced Input. They come either from a UFarmInputConfig
; data asset, or -- when none is assigned -- from UFarmInputConfig::
; CreateRuntimeConfig(), which builds the WASD / mouse / E / Esc mapping
; context in C++. See Source/HayDayFarm/Player/FarmInputConfig.cpp.
;
; Nothing should be added to the legacy ActionMappings/AxisMappings arrays
; below; they exist only because the engine expects the section.

[/Script/Engine.InputSettings]
bAltEnterTogglesFullscreen=True
bCaptureMouseOnLaunch=True
DefaultViewportMouseCaptureMode=CapturePermanently_IncludingInitialMouseDown
DefaultViewportMouseLockMode=LockOnCapture
